TODO:

Move into a component system
Events should be handled in the components, which then sends events?



Move the popup into a component underneath the objects view
Implement properties gets/sets
Show properties in the component underneath